From d4865cb7f4660840af48b6563854f3f8cfcadd01 Mon Sep 17 00:00:00 2001
From: Larry Finger <Larry.Finger@lwfinger.net>
Date: Thu, 23 May 2019 00:14:04 -0500
Subject: [PATCH] rtl8812au: Fix builds through 5.2

Signed-off-by: Larry Finger <Larry.Finger@lwfinger.net>
---
 3rdparty/rtl8812auos_dep/os_intfs.c | 5 ++++-
 1 file changed, 4 insertions(+), 1 deletion(-)

diff --git linux-5.2/3rdparty/rtl8812au/os_dep/os_intfs.c linux-5.2/3rdparty/rtl8812au/os_dep/os_intfs.c
index 0b188de..0449f5d 100644
--- linux-5.2/3rdparty/rtl8812au/os_dep/os_intfs.c
+++ linux-5.2/3rdparty/rtl8812au/os_dep/os_intfs.c
@@ -972,7 +972,10 @@ static unsigned int rtw_classify8021d(struct sk_buff *skb)
 	return dscp >> 5;
 }
 
-#if (LINUX_VERSION_CODE >= KERNEL_VERSION(4, 19, 0))
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(5, 2, 0))
+static u16 rtw_select_queue(struct net_device *dev, struct sk_buff *skb,
+			    struct net_device *sb_dev)
+#elif (LINUX_VERSION_CODE >= KERNEL_VERSION(4, 19, 0))
 static u16 rtw_select_queue(struct net_device *dev, struct sk_buff *skb,
 			    struct net_device *sb_dev,
 			    select_queue_fallback_t fallback)
-- 
2.22.0

