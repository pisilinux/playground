Pardus Offline-Updater

With this tool, you can update packages of your offline Pardus computer.

Dependencies:
python 2.7
PyQt4

Note:
setup process is not ready yet. please use "sudo ./setup.py build" command
and build it. use the executable py's in build dir.
