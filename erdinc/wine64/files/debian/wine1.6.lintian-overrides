# Wine preloader needs static linking to reserve memory and work properly, see 
# the comments at loader/preloader.c for a fuller explanation
wine1.5 binary: statically-linked-binary ./usr/bin/wine-preloader
